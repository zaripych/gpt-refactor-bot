---
'refactor-bot': minor
---

feat(mcp): refactor bot can now spin up its ts-morph and other code tools as an
mcp server.

This is just an experiment at the moment. To spin up the server, run:

```bash
refactor-bot mcp --port 3000
```

See `refactor-bot mcp --help` for more options.
